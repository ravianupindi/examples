1. Remove --start_epoch and --resume, always load most recent checkpoint now
2. --batch_size: now specifies per_rank_batch_size
3. --workers: specifies num_dataloaders exactly, defaults to 0
4. removed rank, gpu, multiprocessing-distributed, dist_url since these are obsolete with torchrun elastic launch